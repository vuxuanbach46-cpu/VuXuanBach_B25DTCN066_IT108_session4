#include <stdio.h>

int main (){
	int a ;
	printf ("moi nhap so nguyen :");
	scanf ("%d",&a);
	if (a % 2 == 0){
		printf ("so vua nhap la so chan ");
	}
	else { 
	printf ("so vua nhap la so le");
	}
	return 0;
	
}
