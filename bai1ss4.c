#include <stdio.h>
int main (){
	int a ; 
	printf ("nhap so nguyen :");
	scanf ("%d",&a);
	if (a >= 0 ){
		printf ("so vua nhap la so duong");
	} else {
		printf ("so vua nhap la so am ");
	}
	
	return 0;
	
}
